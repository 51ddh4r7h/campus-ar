{
  "name": "campus-ar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Campus Film Hunt — a timed AR scavenger hunt. Find three movie sets on campus, catch the clapperboard reveal in world-tracking AR, and put your time on the marquee (8th Wall Engine Binary + Three.js, Tailwind-themed UI).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "deploy": "npm run build && wrangler pages deploy --project-name campus-ar",
    "deploy:preview": "npm run build && wrangler pages deploy --project-name campus-ar --branch preview"
  },
  "dependencies": {
    "@8thwall/engine-binary": "^1.0.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@oxlint/plugins": "^1.80.0",
    "@types/node": "^26.2.0",
    "@types/three": "^0.185.4",
    "autoprefixer": "^10.4.20",
    "oxlint": "^1.80.0",
    "playwright": "^1.62.1",
    "postcss": "^8.5.1",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.9.3",
    "vite": "^8.2.1",
    "wrangler": "^4.124.0"
  }
}
